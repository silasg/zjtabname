[package]
name = "zjtabname"
version = "0.2.0"
edition = "2021"
description = "Zellij plugin that automatically sets tab titles to the focused pane's title"

[dependencies]
zellij-tile = "0.44"

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
